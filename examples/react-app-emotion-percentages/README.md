# Live Assist React Example

Minimal React app demonstrating the Live Assist SDK.

## Setup

```bash
# Start the backend services
cd ../../docker
docker compose up -d

# Install and run the example
npm install
npm run dev
```

Open http://localhost:5173 in your browser. Click the widget to start a Live Assist session.
